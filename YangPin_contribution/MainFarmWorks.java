import java.util.ArrayList;

public class MainFarmWorks {
	Farm farm = new Farm();

		MainFarmWorks() {
		farm.cropList = new ArrayList<Crop>();	
		}
		
	void addCrop() {
		Crop fruitTree = new Crop();
		farm.cropList.add(fruitTree);
	}
	
	c.cropTend(weeding);
	
}
